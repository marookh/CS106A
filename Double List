def main():
    numbers = [1,2,3,4,5]
    for i in range(len(numbers)):
        elem_in_index = numbers[i]
        numbers[i] = 2 * elem_in_index
        print(numbers)
   
        
if __name__ ==  '__main__':
    main()
